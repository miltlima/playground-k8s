apiVersion: v1
kind: Service
metadata:
  name: nginx-ingress-controller

spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      NodePort: 30080
  selector:
      app: nginx-pod
      type: back-end
