apiVesion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx-pod
    type: back-end
spec:
  containers:
  - name: nginx-container
    image: nginx
    ports:
    - containerPort: 8080
      protocol: TCP
